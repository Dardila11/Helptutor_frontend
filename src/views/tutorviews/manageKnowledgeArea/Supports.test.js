import React from 'react'
import { render, fireEvent, screen, cleanup, waitFor } from 'src/test-utils'

import SupportsView from 'src/views/tutorviews/manageKnowledgeArea/knowledgeAreaInfo/supports'

describe('Supports View', () => {
    it.todo('campos obligatorios no se han ingresado')
    it.todo('al subir un archivo, se muestra el texto archivo subido')
})